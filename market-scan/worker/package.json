{
  "name": "esrealty-market-scan-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Durable Market Scan worker for ES Realty: live scraping + Facebook Marketplace (Playwright) + listing history / median benchmarks.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "fb-login": "node -e \"require('./scan-browser.js').login().then(()=>process.exit(0))\""
  },
  "engines": {
    "node": ">=18"
  },
  "optionalDependencies": {
    "playwright": "^1.62.1"
  }
}
